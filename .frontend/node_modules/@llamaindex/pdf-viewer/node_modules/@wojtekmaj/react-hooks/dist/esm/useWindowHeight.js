import { useCallback, useState } from 'react';
import useEventListener from './useEventListener';
var isBrowser = typeof document !== 'undefined';
/**
 * Returns the interior height of the window in pixels.
 *
 * @returns {number | null} Height of the window in pixels
 */
export default function useWindowHeight() {
    var _a = useState(isBrowser ? window.innerHeight : null), windowHeight = _a[0], setWindowHeight = _a[1];
    var getWindowHeight = useCallback(function () { return setWindowHeight(window.innerHeight); }, []);
    useEventListener(isBrowser ? window : null, 'resize', getWindowHeight);
    return windowHeight;
}
