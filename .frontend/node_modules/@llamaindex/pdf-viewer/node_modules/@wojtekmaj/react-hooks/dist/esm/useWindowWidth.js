import { useCallback, useState } from 'react';
import useEventListener from './useEventListener';
var isBrowser = typeof document !== 'undefined';
/**
 * Returns the interior width of the window in pixels.
 *
 * @returns {number | null} Width of the window in pixels
 */
export default function useWindowWidth() {
    var _a = useState(isBrowser ? window.innerWidth : null), windowWidth = _a[0], setWindowWidth = _a[1];
    var getWindowWidth = useCallback(function () { return setWindowWidth(window.innerWidth); }, []);
    useEventListener(isBrowser ? window : null, 'resize', getWindowWidth);
    return windowWidth;
}
