import { useCallback, useState } from 'react';
import useEventListener from './useEventListener';
var isBrowser = typeof document !== 'undefined';
/**
 * Returns the online status of the browser.
 *
 * @returns {boolean | null} Online status of the browser
 */
export default function useOnLine() {
    var _a = useState(isBrowser ? navigator.onLine : null), onLine = _a[0], setOnLine = _a[1];
    var handleOnline = useCallback(function () {
        setOnLine(true);
    }, []);
    var handleOffline = useCallback(function () {
        setOnLine(false);
    }, []);
    useEventListener(isBrowser ? window : null, 'online', handleOnline);
    useEventListener(isBrowser ? window : null, 'offline', handleOffline);
    return onLine;
}
