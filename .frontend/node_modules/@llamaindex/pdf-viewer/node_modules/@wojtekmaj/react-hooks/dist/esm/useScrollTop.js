import { useCallback, useState } from 'react';
import useEventListener from './useEventListener';
var isBrowser = typeof document !== 'undefined';
/**
 * Returns current scroll top position in pixels.
 *
 * @returns {number | null} Scroll top position in pixels
 */
export default function useScrollTop() {
    var _a = useState(isBrowser ? window.pageYOffset : null), scrollTop = _a[0], setScrollTop = _a[1];
    var getScrollTop = useCallback(function () { return setScrollTop(window.pageYOffset); }, []);
    useEventListener(isBrowser ? document : null, 'scroll', getScrollTop);
    return scrollTop;
}
