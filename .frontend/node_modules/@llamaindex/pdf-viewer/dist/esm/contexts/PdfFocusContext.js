import { jsx as _jsx } from "react/jsx-runtime";
import React, { createContext, useState } from 'react';
// Initialize Context
export const PdfFocusContext = createContext(undefined);
// PDF Provider
const PdfFocusProvider = ({ children }) => {
    const [pdfFocusState, setPdfFocusState] = useState({
        documentId: '',
        pageNumber: 0,
    });
    return (_jsx(PdfFocusContext.Provider, { value: {
            pdfFocusState: pdfFocusState,
            setPdfFocusState: setPdfFocusState,
        }, children: children }));
};
export default PdfFocusProvider;
